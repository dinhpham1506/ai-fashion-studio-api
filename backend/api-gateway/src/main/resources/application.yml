server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      # Backend (.NET) cũng tự set header CORS; nếu không dedupe, browser nhận
      # Access-Control-Allow-Origin bị lặp đôi và chặn request.
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials, RETAIN_UNIQUE
      globalcors:
        cors-configurations:
          '[/**]':
            allowed-origins:
              - "http://localhost:3000"
              - "http://localhost:3001"
              - "http://localhost:5173"
            allowed-methods: "*"
            allowed-headers: "*"
            allow-credentials: true

      routes:
        # ---- C# dotnet-platform-api ----
        - id: identity
          uri: ${SVC_DOTNET_URI:http://localhost:8082}
          predicates:
            - Path=/api/auth/**,/api/users/**,/api/roles/**

        - id: payment
          uri: ${SVC_DOTNET_URI:http://localhost:8082}
          predicates:
            - Path=/api/payments/**,/api/invoices/**

        - id: content
          uri: ${SVC_DOTNET_URI:http://localhost:8082}
          predicates:
            - Path=/api/about-us/**

        # Staff Operation Gateway (C# validate role roi goi Java)
        - id: staff-gateway
          uri: ${SVC_DOTNET_URI:http://localhost:8082}
          predicates:
            - Path=/api/staff/**

        - id: feedback
          uri: ${SVC_DOTNET_URI:http://localhost:8082}
          predicates:
            - Path=/api/feedbacks/**

        - id: ai-chat
          uri: ${SVC_DOTNET_URI:http://localhost:8082}
          predicates:
            - Path=/api/ai-chat/**

        # ---- Java java-core-api ----
        - id: catalog
          uri: ${SVC_JAVA_URI:http://localhost:8081}
          predicates:
            - Path=/api/products/**,/api/variants/**,/api/inventory/**,/admin/catalogs/**,/admin/variants/**,/admin/inventory/**

        - id: design-tryon
          uri: ${SVC_JAVA_URI:http://localhost:8081}
          predicates:
            - Path=/api/designs/**,/api/tryon/**

        - id: ordering
          uri: ${SVC_JAVA_URI:http://localhost:8081}
          predicates:
            - Path=/api/orders/**

        # ---- OpenAPI docs from Java service ----
        - id: java-core-openapi
          uri: ${SVC_JAVA_URI:http://localhost:8081}
          predicates:
            - Path=/v3/api-docs/java-core-api
          filters:
            - RewritePath=/v3/api-docs/java-core-api,/v3/api-docs

        # ---- OpenAPI docs from .NET service ----
        - id: platform-openapi
          uri: ${SVC_DOTNET_URI:http://localhost:8082}
          predicates:
            - Path=/v3/api-docs/platform-api
          filters:
            - RewritePath=/v3/api-docs/platform-api,/swagger/v1/swagger.json

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    urls:
      - name: Java Core API
        url: /v3/api-docs/java-core-api
      - name: Platform API
        url: /v3/api-docs/platform-api

management:
  endpoints:
    web:
      exposure:
        include: health,info
